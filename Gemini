-- ====================================================================
--  BẢN SCRIPT BLOX FRUIT FULL SEA ĐÃ TÍCH HỢP ẢNH LOGO: 129898296854108
-- ====================================================================

-- Ép hệ thống chuyển đổi ID ảnh của bạn sang dạng Content ID chuẩn của Roblox
local MY_LOGO_ID = "rbxassetid://129898296854108"

-- [MODULE 1: ANTI-AFK CHỐNG DISCONNECT]
local VirtualUser = game:GetService("VirtualUser")
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new(0,0))
end)

_G.AutoFarm = false
_G.AutoStats = false
_G.SelectStat = "Melee"
_G.FastAttack = true

local Player = game.Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")

-- [MODULE 2: TWEEN BYPASS ANTI-CHEAT]
function TweenTo(TargetCFrame)
    local Character = Player.Character
    if not Character or not Character:FindFirstChild("HumanoidRootPart") then return end
    
    local Distance = (TargetCFrame.Position - Character.HumanoidRootPart.Position).Magnitude
    local Speed = 350 
    local Info = TweenInfo.new(Distance / Speed, Enum.EasingStyle.Linear)
    
    local Tween = TweenService:Create(Character.HumanoidRootPart, Info, {CFrame = TargetCFrame})
    Tween:Play()
    
    local NoClipConnection
    NoClipConnection = game:GetService("RunService").Stepped:Connect(function()
        if Tween.PlaybackState == Enum.PlaybackState.Playing then
            for _, part in pairs(Character:GetChildren()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        else
            NoClipConnection:Disconnect()
        end
    end)
    Tween.Completed:Wait()
end

-- [MODULE 3: AUTO FARM LOGIC CHO CẢ 3 SEAS]
function GetQuestDetails()
    local Level = Player.Data.Level.Value
    local MySea = game.PlaceId
    
    -- Khu vực Sea 1
    if MySea == 2753915541 then
        if Level >= 1 and Level < 10 then
            return "BanditQuest1", "Bandit", CFrame.new(1059, 15, 1549), CFrame.new(1145, 17, 1634), 1
        elseif Level >= 10 and Level < 15 then
            return "MonkeyQuest1", "Monkey", CFrame.new(-1598, 36, 153), CFrame.new(-1610, 37, 146), 1
        end
        return "BanditQuest1", "Bandit", CFrame.new(1059, 15, 1549), CFrame.new(1145, 17, 1634), 1

    -- Khu vực Sea 2
    elseif MySea == 4442272160 then
        if Level >= 700 and Level < 775 then
            return "Area1Quest", "Raider", CFrame.new(-424, 73, 1836), CFrame.new(-504, 73, 1912), 1
        end
        return "Area1Quest", "Raider", CFrame.new(-424, 73, 1836), CFrame.new(-504, 73, 1912), 1

    -- Khu vực Sea 3
    elseif MySea == 7405815058 then
        if Level >= 1500 and Level < 1575 then
            return "PiratePortQuest", "Pirate Millionaire", CFrame.new(-290, 15, 5427), CFrame.new(-340, 15, 5510), 1
        end
        return "PiratePortQuest", "Pirate Millionaire", CFrame.new(-290, 15, 5427), CFrame.new(-340, 15, 5510), 1
    end
end

task.spawn(function()
    while true do
        task.wait(0.1)
        if _G.AutoFarm then
            pcall(function()
                local QuestName, MonsterName, NPCCFrame, MonsterCFrame, QuestID = GetQuestDetails()
                local Character = Player.Character
                local HasQuest = string.find(Player.PlayerGui.Main.Quest.Container.QuestTitle.Text, MonsterName)
                
                if not HasQuest then
                    TweenTo(NPCCFrame)
                    task.wait(0.3)
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("StartQuest", QuestName, QuestID)
                else
                    local TargetMonster = nil
                    for _, enemy in pairs(game.Workspace.Enemies:GetChildren()) do
                        if enemy.Name == MonsterName and enemy:FindFirstChild("HumanoidRootPart") and enemy.Humanoid.Health > 0 then
                            TargetMonster = enemy
                            break
                        end
                    end
                    
                    if TargetMonster then
                        Character.HumanoidRootPart.CFrame = TargetMonster.HumanoidRootPart.CFrame * CFrame.new(0, 11, 0)
                        for _, m in pairs(game.Workspace.Enemies:GetChildren()) do
                            if m.Name == MonsterName and m:FindFirstChild("HumanoidRootPart") then
                                m.HumanoidRootPart.CFrame = TargetMonster.HumanoidRootPart.CFrame
                                m.HumanoidRootPart.CanCollide = false
                            end
                        end
                        
                        local Tool = Player.Backpack:FindFirstChild("Combat") or Character:FindFirstChild("Combat")
                        if Tool then
                            Character.Humanoid:EquipTool(Tool)
                            if _G.FastAttack then
                                ReplicatedStorage.Remotes.Validator:FireServer(math.random(1, 9999))
                                Tool:Activate()
                            end
                        end
                    else
                        TweenTo(MonsterCFrame)
                    end
                end
            end)
        end
    end
end)

-- [MODULE 4: AUTO STATS WORKER]
task.spawn(function()
    while true do
        task.wait(0.5)
        if _G.AutoStats then
            pcall(function()
                ReplicatedStorage.Remotes.CommF_:InvokeServer("AddPoint", _G.SelectStat, 1)
            end)
        end
    end
end)

-- [MODULE 5: KHỞI TẠO MENU UI MỚI (KAVO UI) - KHẮC PHỤC LỖI KHÔNG HIỆN MENU]
local KavoLib = loadstring(game:HttpGet("https://githubusercontent.com"))()
local Window = KavoLib.CreateLib("⚡ PREMIUM BLOX FRUIT HUB", "BloodTheme") -- Giao diện đỏ đen cực ngầu

-- Tạo một khu vực hiển thị hình ảnh Logo của bạn ngay trên Menu chính
local MainTab = Window:NewTab("Tổng Quan")
local InfoSection = MainTab:NewSection("Thông Tin Bản Quyền")

-- Đưa ảnh của bạn vào UI dưới dạng nhãn hiển thị hình ảnh
pcall(function()
    local ImageLabel = Instance.new("ImageLabel")
    ImageLabel.Size = UDim2.new(0, 120, 0, 120)
    ImageLabel.Position = UDim2.new(0.35, 0, 0, 10)
    ImageLabel.Image = MY_LOGO_ID
    ImageLabel.BackgroundTransparency = 1
    -- Tìm khung Core UI của Kavo để chèn ảnh của bạn vào
    ImageLabel.Parent = game:GetService("CoreGui"):WaitForChild("⚡ PREMIUM BLOX FRUIT HUB"):FindFirstChild("Main")
end)

InfoSection:NewLabel("Ảnh cấu hình thành công ID: 129898296854108")

-- CÁC TAB CHỨC NĂNG CÀY CUỐC
local FarmSection = Window:NewTab("Auto Farm"):NewSection("Cày Cấp")

FarmSection:NewToggle("Bật Auto Farm Level", "Tự động chạy đa bản đồ 3 Sea", function(state)
    _G.AutoFarm = state
end)

FarmSection:NewToggle("Đấm Siêu Tốc (Fast Attack)", "Xóa hoạt ảnh đấm để tăng tốc sát thương", function(state)
    _G.FastAttack = state
end)

local StatsSection = Window:NewTab("Auto Stats"):NewSection("Nâng Chỉ Số")

StatsSection:NewDropdown("Chọn Thuộc Tính", "Chọn điểm cần nâng", {"Melee", "Defense", "Sword", "Demon Fruit"}, function(currentOption)
    _G.SelectStat = currentOption
end)

StatsSection:NewToggle("Bật Tự Động Nâng Điểm", "Tự cộng điểm ngay khi lên cấp", function(state)
    _G.AutoStats = state
end)
